/*import feedService from '../../src/services/feed';

export default async function handler(
  req,
  res,
) {
    const recipes = await feedService.getAll();

    console.log('recipes', recipes)
    return res.status(200).json(recipes);
}
*/